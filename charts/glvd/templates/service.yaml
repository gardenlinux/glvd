apiVersion: v1
kind: Service
metadata:
  name: {{ include "glvd.fullname" . }}
  labels:
    {{- include "glvd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "glvd.selectorLabels" . | nindent 4 }}
