meta {
  name: get-root
  type: http
  seq: 2
}

get {
  url: {{base-URL}}
  body: json
  auth: none
}

tests {
  test("should be able to get start page", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
